const intros=["AI write-up, which means a robot watched this so your attention span didn’t have to.","A machine reviewed this video and is now suspiciously confident about grow gear."];
const closers=["Useful? Yes. Stable? Debatable.","Somewhere between genuinely helpful and deeply unserious, which feels correct."];
function guessFocus(title:string,tags:string[]){const l=title.toLowerCase(); if(l.includes("ac infinity")) return "AC Infinity gear, setup choices, and environmental control decisions that can absolutely consume an evening"; if(tags.includes("humidifier")) return "humidity control and trying to keep plants happy without turning the room into a swamp"; if(tags.includes("controller")) return "controllers, automation, and the eternal quest to make your grow room smarter than you are"; return "grow gear, setup decisions, and at least one moment where your wallet feels personally attacked";}
export function generateAISummary(input:{title:string;tags:string[]}){const intro=intros[Math.floor(Math.random()*intros.length)]; const closer=closers[Math.floor(Math.random()*closers.length)]; const focus=guessFocus(input.title,input.tags); return `${intro} In this video, 420Things gets into ${focus}. Expect a mix of actual useful info, a few opinions with some bite, and the general sense that if a product deserves to be roasted, it probably will be. ${closer}`;}
