import videos from "@/data/videos.json"; import { Video } from "@/lib/types"; import SearchAndFilters from "@/components/SearchAndFilters";
export default function VideosPage(){const allVideos=videos as Video[]; return <div className="space-y-6"><div className="space-y-3"><p className="text-sm font-semibold uppercase tracking-wider text-lime">All videos</p><h1 className="text-3xl font-black md:text-5xl">The full 420Things archive</h1><p className="max-w-3xl text-base leading-7 text-muted">Search by topic, tag, year, or whatever vague memory you have of “that one controller video with the rant.”</p></div><SearchAndFilters videos={allVideos} /></div>}