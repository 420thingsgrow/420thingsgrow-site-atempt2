import Link from "next/link"; import { formatDate } from "@/lib/utils"; import { Video } from "@/lib/types";
export default function VideoCard({video}:{video:Video}){return <article className="group overflow-hidden rounded-3xl border border-line bg-panel transition hover:-translate-y-0.5 hover:border-lime/40"><Link href={`/videos/${video.slug}`} className="block"><div className="aspect-video overflow-hidden bg-panel2"><img src={video.thumbnail} alt={video.title} className="h-full w-full object-cover transition duration-300 group-hover:scale-[1.02]" /></div></Link><div className="space-y-3 p-4"><div className="flex flex-wrap gap-2">{video.tags.slice(0,3).map(tag=><span key={tag} className="rounded-full border border-line bg-panel2 px-2.5 py-1 text-xs font-medium text-muted">{tag}</span>)}</div><div><Link href={`/videos/${video.slug}`}><h3 className="text-lg font-bold leading-snug text-text transition group-hover:text-lime">{video.title}</h3></Link><p className="mt-1 text-sm text-muted">{formatDate(video.publishedAt)}</p></div><div className="rounded-2xl border border-line bg-panel2 p-3"><p className="mb-1 text-xs font-semibold uppercase tracking-wide text-pink">AI write-up</p><p className="text-sm leading-6 text-text/90">{video.aiSummary}</p></div></div></article>}